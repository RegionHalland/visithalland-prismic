import React from 'react'
import langs from '../utils/langs'
import Footer from './Footer'

const FooterContainer = ({ meta }) => {
	return <Footer meta={meta} langs={langs} />
}

export default FooterContainer
